<div data-id="{{movie.id}}">

  <h5>
    {{movie.title}}
  </h5>
  <h6>
    id: {{movie.id}}
  </h6>
  <form class="destroy" data-id="{{movie.id}}">
    <fieldset>
      <input type="submit" name="submit" value="Remove From Favorites">
    </fieldset>
  </form>
  </div>

</div>
